# Browser Configuration
browser.name=chrome
browser.headless=false

# Grid Configuration
grid.mode=local
grid.local.url=http://localhost:4444
grid.remote.url=http://your-remote-grid-url:4444

# Docker Configuration
docker.image=playwright:latest
docker.container.name=playwright-grid
docker.port=4444

# API Configuration
api.base.url=http://your-api-base-url 